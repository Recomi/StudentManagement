<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="site.recomi.studentmanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.activities.CampusAssociationActivity" ></activity>
        <activity android:name=".gui.activities.ClassScheduleActivity" />
        <activity android:name=".gui.activities.BrowserActivity" />
        <activity
            android:name=".gui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.activities.GuideActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.activities.WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.activities.WriteNoteActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gui.activities.LoginActivity"
            android:label="@string/login_acount"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".gui.activities.AccountActivity" />
    </application>

</manifest>